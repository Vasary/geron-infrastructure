redis:
  architecture: standalone
  auth:
    enabled: true
    password: "password"

  master:
    persistence:
      enabled: true
      size: 10Gi
      storageClass: "nfs-client"

  replica:
    replicaCount: 1
